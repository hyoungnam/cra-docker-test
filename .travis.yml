sudo: required

language: generic

services:
  - docker

before_install:
  - echo "start creating an image with dockerfile"
  - docker build -t sdc99138/react-docker-9 -f Dockerfile.dev .

script:
  - docker run -e CI=true sdc99138/react-docker-9 npm run test -- --coverage

after_success:
  - echo "Test Success"
